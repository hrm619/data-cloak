[project]
name = "data-cloak"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.1",
]

[dependency-groups]
dev = [
    "faker>=40.8.0",
    "pyarrow>=23.0.1",
    "pytest>=9.0.2",
    "pytest-html>=4.2.0",
]
